# 002 API

## Purpose
Define externally visible API contracts (HTTP, RPC, CLI, events).

## Template
- Interface type:
- Endpoints/commands:
- Request schema:
- Response schema:
- Error model:
- Versioning policy:

## Sample
Interface type: CLI/document API only (no runtime service API in this sample repo).
Endpoints/commands: N/A.
Request schema: N/A.
Response schema: N/A.
Error model: Validation errors should include actionable guidance.
Versioning policy: Keep docs backward compatible where practical.
